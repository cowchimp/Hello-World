# Development Notes

This repo originally contained only a `README` file. A minimal Node.js HTTP server (`server.js`) was added to serve the README content as a web page on port 3000.

## Running

```bash
docker compose -f docker-compose.base44.yml up -d
```

No external services or secrets are needed.
