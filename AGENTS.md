# Development Notes

This repository originally contained only a README. A minimal static HTML page (`index.html`) is served via nginx on port 3000.

## Running locally

```bash
docker compose -f docker-compose.base44.yml up -d
```

Then visit http://localhost:3000.

## No external secrets required
