# Hello-World

This is the classic octocat/Hello-World repo. It originally contained only a `README` file.

## Base44 Setup

- A static `index.html` page was added in `public/` and is served by nginx on port 3000.
- No external services or secrets required.
- To verify: `curl http://localhost:3000/` should return the HTML page.
